 <div class="ui grid">
        <aside class="ui column">
        {{#each tweets}}
            <section class="ui raised segment">
                <form id="dT" action="/timeline" method="POST">
                <div class="ui grid">
                    <aside class="ui twelve wide column">{{firstName}} {{lastName}}</aside>
                    <aside class="ui four wide column">{{timeUI}}</aside>
                </div>
                <div class="ui grid">
                    <aside class="ui sixteen wide column">
                        <div class="ui inverted divider"></div>
                    </aside>
                    <aside class="ui twelve wide column">{{tweetText}}</aside>
                    <aside class="ui three wide column">
                        <button class="ui red button">
                        <i class="centered black trash icon"></i>
                        </button>
                    </aside>
                    <input type="hidden" value={{_id}} name="Object_id" />
                </div>
                </form>
            </section>
        {{/each}}
        </aside>
    </div>
